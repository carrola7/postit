<%= render 'shared/form_error', form: @category %>

<div class='well'>
  <%= form_for @category do |f| %>
    <div class='control-group'>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <br>
    <%= submit_tag btn_txt, class: 'btn btn-primary' %>
  <% end %>
</div>